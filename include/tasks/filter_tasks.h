#pragma once
#include <bricks/Filter_handler.h>

namespace filterTasks
{





     void updateFilter(void* filter)
    {
        // controller::weightedSumFilter* localFilter = (controller::weightedSumFilter* ) filter;

        // xQueueSendToBack(localFilter->q_handle, (void *) &redLEDFlyCount, (TickType_t) 10); // should maybe check that it was correctly sent




    }
}